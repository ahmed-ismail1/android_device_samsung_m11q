# Allow fingerprint HAL to access Silead fingerprint device
allow hal_fingerprint_default silead_fp_device:chr_file rw_file_perms;

# Allow fingerprint HAL to communicate with QSEECom/TEE
allow hal_fingerprint_default tee_device:chr_file rw_file_perms;
